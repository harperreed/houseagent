# ABOUTME: Dockerfile for HouseAgent MQTT agent service
# ABOUTME: Uses UV for dependency management and runtime

FROM ghcr.io/astral-sh/uv:python3.12-bookworm-slim

WORKDIR /app

# Copy everything
COPY . .

# Run with uv (handles dependencies automatically)
CMD ["uv", "run", "agent.py"]
